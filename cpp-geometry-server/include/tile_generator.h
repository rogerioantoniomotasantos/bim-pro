#pragma once
#include <memory>
#include "ifc_processor.h"

namespace bim { namespace geometry {
class TileGenerator {
public:
    TileGenerator() = default;
    ~TileGenerator() = default;
};
}} // namespace
